<?xml version="1.0" encoding="utf-8"?>
<!--
  Cloud backup is disabled entirely; device-to-device transfer is allowed.

  Result: a user's board survives a new phone, and nothing ever reaches a cloud
  provider. This matches the app's promise BETTER than conditioning upload on
  encryption, because encrypted cloud backups still leave the device and still
  depend on the user having a screen lock — which this population may not.

  The threat model is not abstract: phrases like "I am being hurt" imply an
  adversary who may be a caregiver or partner with access to the account.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <include domain="database" path="." />
        <include domain="file" path="." />
        <include domain="sharedpref" path="." />
    </device-transfer>
</data-extraction-rules>
