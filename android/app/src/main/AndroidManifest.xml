<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- NEVER set android:allowBackup="true", and never delete the
         dataExtractionRules line below. Both together are the ONLY thing that
         keeps the board off Google Drive. The default (true) uploads the SQLite
         database — every phrase the user has ever curated — to an account a
         caregiver or partner may hold. The threat model is not abstract: a board
         holds phrases like "I am being hurt". This is a safety accommodation,
         not a preference, and it is one line of XML from being silently untrue.
         Do NOT "improve" it into an encrypted cloud backup: encrypted backups
         still leave the device and still assume a screen lock this population
         may not have. Off is a stronger promise than encrypted.
         A policy test (test/policy/android_manifest_policy_test.dart) fails the
         moment this regresses. -->
    <application
        android:label="Reed"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <!-- Android 11+ package visibility hides the TTS engine without this.
             Remove it and getVoices() returns an empty list: the app is silent
             on every device, with no error anywhere. A policy test guards it. -->
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
